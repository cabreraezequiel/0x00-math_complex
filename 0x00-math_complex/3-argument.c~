#include "holberton.h"
#include <math.h>

/**
 * argument - returns the argument of a given complex number
 * @c: complex number c
 * Return: argument
 */

double argument(complex c);
{
	double result;
	double a = pow(c.re, 2);
	double b = pow(c.im, 2);

	result = sqrt(a + b);

	return(result);
}
